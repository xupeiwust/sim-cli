driver: gmsh
sdk_package: gmsh

profiles:
  - name: gmsh_4_15
    solver_versions: ["4.15"]
    active_sdk_layer: null
    active_solver_layer: "4.15"
    notes: |
      Gmsh 4.15.2 from the pip `gmsh` wheel. Self-contained install
      (CLI wrapper + Python module). E2E verified with OpenCASCADE
      unit sphere → 258 nodes, 1278 tet elements (msh22 format).
